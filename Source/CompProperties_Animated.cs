using Verse;

namespace AnimalAnimationFramework
{
    public class CompProperties_Animated : CompProperties
    {
        public CompProperties_Animated()
        {
            this.compClass = typeof(CompAnimated);
        }
    }
}